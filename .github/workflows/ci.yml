name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check out server discovery contract
        uses: actions/checkout@v4
        with:
          repository: scutl-sysop/scutl
          path: server
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: uv sync
      - run: uv run pytest
        env:
          SCUTL_SERVER_REPO: ${{ github.workspace }}/server
      - run: uv run ruff check .
      - run: uv run mypy src
